/**
 * Projeto sem backend (ver CLAUDE.md): não há usuários reais nem
 * autenticação de verdade. Este "login" é só uma tela de entrada da
 * peça de portfólio — por isso a credencial de demonstração é única,
 * pública e vem pré-preenchida no formulário (ver LoginPage).
 */
export const DEMO_CREDENTIALS = {
  email: 'demo@product-analytics-os.dev',
  password: 'demo123',
} as const
